i still dont have a "sever patient data button in the admin menu bar drop down. 

the patient header in the patient chart is so messy. 

The name is in the right spot but should be "LASTNAME, FIRST NAME (MRN)" with ##_YEARS_OLD (and) DOB underneath the name and MRN 

Edit layout should be a small almost hidden button, just like the widgets small settings gear icon. i don tneed to see view or layout options without going into a settings window. 

Allergies should be centered in the header, should be a badge that takes up 1/4 of the banner width, and has a reasonable margin from the edges of the header. the opposite side from the name should be three lines "VIIS checked: DATE" "PDMP checked: date" "Last Synced: Date" the first letters of the three lines should all be in a vertical row, and the first number of the dates should be in a vertical row. clicking the text should then initiate a manual pull of viis, pdmp, or search for updated xml

There should be buttons between the allergies badge and the "last synced" information that has three rows, Print, Claim, Settings

The tabs for overview, meds, labs,, problems, notes, billing, calculators, and any others that may come in the future... and ANY OTHER SPOT IN OUR PROGRAM should be like the tabs in a filing cabinet... like the tabs show in google chrome. they should stick up and stick out, connecting to the content below them to make it look like one big wrapper for the header and content. that way we know EXACTLY what tab is showing what content. Any tabs should be centered and even spaced in whatever window or widget they are being displayed in. 

The medication widget should not have "dose/form" it should just be "Dose"

when inactivating a medication from the widget we should have a 5 second "fade to black" where the user can click the (in)activate button again and keep it in the active list. if they do not click it in 5 seconds, it disappears from the list and moved to the inactive list. 

i dont want a drop down for the all/active/inactive.. i want tabs like described above.

We should be able to edit and save any changes to medications from this widget, and the changes should persist log out until the patients data is archived. it should persist a new XML upload as well, but have a small yellow flag and a hover over showing there may be a discrepancy between carecompanion and the amazing charts XML data. 

in the problems tab, the diagnoses are not resizing to fit the sidth of the dignoses widget. all widets, windows, wct should auto snap to fill. diagnoses should have all/active/inactive tabs. diagnoses widget what two settings buttons, one is small with clear background, the other is a large white button. This white button should get combined into the copy button with a " | " character and a small downwards arrow to indicate there are copy options. 

in our billing data csv we had a MASSIVE list of ICD 10 codes, we should use these to supplement the ICD10 API to help speed up this next request... in the lookup icd10 function,w e should be able to begin typing an ddisplay a list of the matching diagonises, filtering further as we type. the focus should be on the first/top result in this list so the user can hit enter and it will already be selected. 